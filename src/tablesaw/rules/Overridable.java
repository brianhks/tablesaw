package tablesaw.rules;

/**
	This is a special interface for pre added rules that can be overridden by
	the user without explicitly calling the override method on the rule
*/
public interface Overridable
	{
	}
